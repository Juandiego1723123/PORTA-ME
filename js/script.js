const translations = {
  en: {
    'nav.home': 'Home', 'nav.about': 'About', 'nav.skills': 'Skills', 'nav.projects': 'Projects',
    'nav.certifications': 'Certifications', 'nav.education': 'Education', 'nav.contact': 'Contact',
    'hero.greeting': "Hi, I'm",
    'hero.role1': 'Frontend Developer', 'hero.role2': 'Systems Engineering Student',
    'hero.desc': 'I build modern, responsive and user-friendly web applications with clean code and great user experience. Passionate about turning ideas into real, functional products.',
    'hero.viewProjects': 'View Projects', 'hero.downloadCV': 'Download CV', 'hero.contactMe': 'Contact Me',
    'about.title': 'About Me',
    'about.p1': 'I am a Systems Engineering student passionate about software development, web technologies and artificial intelligence. I enjoy creating solutions that solve real-world problems through technology.',
    'about.p2': "I am proactive, responsible and always eager to learn new technologies and take on new challenges. My goal is to build software that's not only functional but genuinely pleasant to use.",
    'about.stat1': 'Projects<br>Completed', 'about.stat2': 'Technologies<br>Learned',
    'about.stat3': 'Certifications<br>Earned', 'about.stat4': 'Dedication<br>&amp; Passion',
    'skills.title': 'Skills & Technologies', 'skills.frontend': 'Frontend', 'skills.backend': 'Backend',
    'skills.databases': 'Databases', 'skills.tools': 'Tools',
    'projects.title': 'Featured Projects',
    'contractiq.desc': 'Enterprise platform to centralize, manage and automate the contract lifecycle — vendors, contractors, documents and contracts from a single dashboard, with an AI assistant (Ollama + Llama 3.2) that analyzes PDF contracts and extracts key information.',
    'smartops.desc': 'Intelligent platform to optimize inventory, sales and business performance for SMEs, with real-time dashboards, PDF/Excel reports and an AI assistant (Ollama + Llama 3.2) that auto-registers products, analyzes stock risk and gives smart recommendations.',
    'mars.desc': 'Responsive web app to showcase jewelry accessories, with an elegant, mobile-first catalog experience, detailed product views, smooth page navigation and a dynamic JavaScript-driven collection.',
    'certifications.title': 'Certifications',
    'education.title': 'Education', 'edu1.degree': "Bachelor's Degree in Systems Engineering",
    'edu2.degree': 'English Program',
    'education.currentlyLearning': 'Currently Learning',
    'learning.tag1': 'Advanced React', 'learning.tag5': 'Artificial Intelligence', 'learning.tag6': 'Software Architecture',
    'contact.title': "Let's Connect!",
    'contact.sub': "I'm open to new opportunities and excited to work on innovative projects.",
    'contact.name': 'Name', 'contact.namePlaceholder': 'Your name',
    'contact.email': 'Email', 'contact.emailPlaceholder': 'your@email.com',
    'contact.message': 'Message', 'contact.messagePlaceholder': 'Tell me about your project...',
    'contact.send': 'Send Message',
    'contact.sending': 'Sending...',
    'contact.formSuccess': "Thanks! Your message has been sent — I'll get back to you soon.",
    'contact.formError': 'Something went wrong sending your message. Please try again or email me directly.',
    'footer.rights': 'All rights reserved.',
  },
  es: {
    'nav.home': 'Inicio', 'nav.about': 'Sobre Mí', 'nav.skills': 'Habilidades', 'nav.projects': 'Proyectos',
    'nav.certifications': 'Certificaciones', 'nav.education': 'Educación', 'nav.contact': 'Contacto',
    'hero.greeting': 'Hola, soy',
    'hero.role1': 'Desarrollador Frontend', 'hero.role2': 'Estudiante de Ingeniería de Sistemas',
    'hero.desc': 'Construyo aplicaciones web modernas, responsivas y fáciles de usar, con código limpio y una gran experiencia de usuario. Apasionado por convertir ideas en productos reales y funcionales.',
    'hero.viewProjects': 'Ver Proyectos', 'hero.downloadCV': 'Descargar CV', 'hero.contactMe': 'Contáctame',
    'about.title': 'Sobre Mí',
    'about.p1': 'Soy estudiante de Ingeniería de Sistemas, apasionado por el desarrollo de software, las tecnologías web y la inteligencia artificial. Disfruto creando soluciones que resuelven problemas reales a través de la tecnología.',
    'about.p2': 'Soy proactivo, responsable y siempre estoy dispuesto a aprender nuevas tecnologías y asumir nuevos retos. Mi objetivo es construir software que no solo funcione, sino que sea realmente agradable de usar.',
    'about.stat1': 'Proyectos<br>Completados', 'about.stat2': 'Tecnologías<br>Aprendidas',
    'about.stat3': 'Certificaciones<br>Obtenidas', 'about.stat4': 'Dedicación<br>y Pasión',
    'skills.title': 'Habilidades y Tecnologías', 'skills.frontend': 'Frontend', 'skills.backend': 'Backend',
    'skills.databases': 'Bases de Datos', 'skills.tools': 'Herramientas',
    'projects.title': 'Proyectos Destacados',
    'contractiq.desc': 'Plataforma empresarial para centralizar, administrar y automatizar el ciclo de vida de contratos — proveedores, contratistas, documentos y contratos desde un solo panel, con un asistente de IA (Ollama + Llama 3.2) que analiza contratos en PDF y extrae información clave.',
    'smartops.desc': 'Plataforma inteligente para optimizar el inventario, las ventas y el rendimiento del negocio en pymes, con dashboards en tiempo real, reportes en PDF/Excel y un asistente de IA (Ollama + Llama 3.2) que registra productos automáticamente, analiza riesgos de stock y da recomendaciones inteligentes.',
    'mars.desc': 'Aplicación web responsive para la exhibición de accesorios de joyería, con una experiencia de catálogo elegante y mobile-first, vista detallada de cada producto, navegación fluida y un catálogo dinámico generado con JavaScript.',
    'certifications.title': 'Certificaciones',
    'education.title': 'Educación', 'edu1.degree': 'Ingeniería de Sistemas (Pregrado)',
    'edu2.degree': 'Programa de Inglés',
    'education.currentlyLearning': 'Actualmente Aprendiendo',
    'learning.tag1': 'React Avanzado', 'learning.tag5': 'Inteligencia Artificial', 'learning.tag6': 'Arquitectura de Software',
    'contact.title': '¡Conectemos!',
    'contact.sub': 'Estoy abierto a nuevas oportunidades y emocionado por trabajar en proyectos innovadores.',
    'contact.name': 'Nombre', 'contact.namePlaceholder': 'Tu nombre',
    'contact.email': 'Correo', 'contact.emailPlaceholder': 'tu@correo.com',
    'contact.message': 'Mensaje', 'contact.messagePlaceholder': 'Cuéntame sobre tu proyecto...',
    'contact.send': 'Enviar Mensaje',
    'contact.sending': 'Enviando...',
    'contact.formSuccess': '¡Gracias! Tu mensaje fue enviado — te responderé pronto.',
    'contact.formError': 'Algo salió mal al enviar tu mensaje. Intenta de nuevo o escríbeme directamente por correo.',
    'footer.rights': 'Todos los derechos reservados.',
  }
};

function applyLanguage(lang){
  document.documentElement.lang = lang;
  document.querySelectorAll('[data-i18n]').forEach(el => {
    const value = translations[lang][el.dataset.i18n];
    if (value !== undefined) el.textContent = value;
  });
  document.querySelectorAll('[data-i18n-html]').forEach(el => {
    const value = translations[lang][el.dataset.i18nHtml];
    if (value !== undefined) el.innerHTML = value;
  });
  document.querySelectorAll('[data-i18n-placeholder]').forEach(el => {
    const value = translations[lang][el.dataset.i18nPlaceholder];
    if (value !== undefined) el.placeholder = value;
  });
  const langToggle = document.getElementById('langToggle');
  if (langToggle) langToggle.textContent = lang === 'en' ? 'ES' : 'EN';
  const downloadCvBtn = document.getElementById('downloadCvBtn');
  if (downloadCvBtn) downloadCvBtn.href = lang === 'es'
    ? 'imagenes/CV-Juan-Diego-Edna-Smalbach-ES.pdf'
    : 'imagenes/CV-Juan-Diego-Edna-Smalbach-EN.pdf';
  localStorage.setItem('lang', lang);
}

document.addEventListener('DOMContentLoaded', () => {

  /* ===== Language gate ===== */
  const langGate = document.getElementById('langGate');
  const savedLang = localStorage.getItem('lang');
  if (savedLang){
    applyLanguage(savedLang);
    langGate.classList.add('hidden');
  }
  langGate.querySelectorAll('.lang-gate-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      applyLanguage(btn.dataset.lang);
      langGate.classList.add('hidden');
    });
  });

  document.getElementById('langToggle').addEventListener('click', () => {
    const next = document.documentElement.lang === 'en' ? 'es' : 'en';
    applyLanguage(next);
  });

  /* ===== Preloader ===== */
  const preloader = document.getElementById('preloader');
  window.addEventListener('load', () => {
    setTimeout(() => preloader.classList.add('hidden'), 1200);
  });
  // fallback in case 'load' already fired or is slow
  setTimeout(() => preloader.classList.add('hidden'), 3000);

  /* ===== Reduced motion preference ===== */
  const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  /* ===== Custom cursor ===== */
  const cursorDot = document.getElementById('cursorDot');
  const cursorOutline = document.getElementById('cursorOutline');
  const spotlight = document.getElementById('spotlight');
  let mouseX = 0, mouseY = 0, outlineX = 0, outlineY = 0;

  window.addEventListener('mousemove', (e) => {
    mouseX = e.clientX; mouseY = e.clientY;
    cursorDot.style.left = mouseX + 'px';
    cursorDot.style.top = mouseY + 'px';
    spotlight.style.setProperty('--x', mouseX + 'px');
    spotlight.style.setProperty('--y', mouseY + 'px');
  });

  const outlineEase = prefersReducedMotion ? 1 : 0.15;
  function animateOutline(){
    outlineX += (mouseX - outlineX) * outlineEase;
    outlineY += (mouseY - outlineY) * outlineEase;
    cursorOutline.style.left = outlineX + 'px';
    cursorOutline.style.top = outlineY + 'px';
    requestAnimationFrame(animateOutline);
  }
  animateOutline();

  document.querySelectorAll('a, button, .skill-item, .project-card, input, textarea').forEach(el => {
    el.addEventListener('mouseenter', () => cursorOutline.classList.add('grow'));
    el.addEventListener('mouseleave', () => cursorOutline.classList.remove('grow'));
  });

  /* ===== Particles background ===== */
  const canvas = document.getElementById('particles');
  const ctx = canvas.getContext('2d');
  let particles = [];
  let width, height;

  function resizeCanvas(){
    width = canvas.width = window.innerWidth;
    height = canvas.height = window.innerHeight;
  }
  resizeCanvas();
  window.addEventListener('resize', resizeCanvas);

  function createParticles(){
    const count = Math.min(80, Math.floor((width * height) / 18000));
    const speed = prefersReducedMotion ? 0 : 0.3;
    particles = Array.from({ length: count }, () => ({
      x: Math.random() * width,
      y: Math.random() * height,
      vx: (Math.random() - 0.5) * speed,
      vy: (Math.random() - 0.5) * speed,
      r: Math.random() * 1.8 + 0.6
    }));
  }
  createParticles();
  window.addEventListener('resize', createParticles);

  const isLight = () => document.body.getAttribute('data-theme') === 'light';

  function drawParticles(){
    ctx.clearRect(0, 0, width, height);
    const dotColor = isLight() ? 'rgba(234,88,12,0.35)' : 'rgba(251,146,60,0.5)';
    const lineColor = isLight() ? 'rgba(234,88,12,' : 'rgba(249,115,22,';

    for (const p of particles){
      p.x += p.vx; p.y += p.vy;
      if (p.x < 0 || p.x > width) p.vx *= -1;
      if (p.y < 0 || p.y > height) p.vy *= -1;
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fillStyle = dotColor;
      ctx.fill();
    }
    for (let i = 0; i < particles.length; i++){
      for (let j = i + 1; j < particles.length; j++){
        const dx = particles[i].x - particles[j].x;
        const dy = particles[i].y - particles[j].y;
        const dist = Math.sqrt(dx*dx + dy*dy);
        if (dist < 120){
          ctx.beginPath();
          ctx.strokeStyle = lineColor + (1 - dist / 120) * 0.15 + ')';
          ctx.lineWidth = 1;
          ctx.moveTo(particles[i].x, particles[i].y);
          ctx.lineTo(particles[j].x, particles[j].y);
          ctx.stroke();
        }
      }
    }
    requestAnimationFrame(drawParticles);
  }
  drawParticles();

  /* ===== Navbar scroll state + active link ===== */
  const navbar = document.getElementById('navbar');
  const sections = document.querySelectorAll('main section[id]');
  const navLinks = document.querySelectorAll('.nav-link');

  function onScroll(){
    navbar.classList.toggle('scrolled', window.scrollY > 40);

    let current = sections[0] ? sections[0].id : '';
    sections.forEach(sec => {
      if (window.scrollY >= sec.offsetTop - 140) current = sec.id;
    });
    navLinks.forEach(link => {
      link.classList.toggle('active', link.getAttribute('href') === '#' + current);
    });

    document.getElementById('backToTop').classList.toggle('show', window.scrollY > 500);
  }
  window.addEventListener('scroll', onScroll);
  onScroll();

  /* ===== Mobile menu ===== */
  const hamburger = document.getElementById('hamburger');
  const navLinksEl = document.getElementById('navLinks');
  hamburger.addEventListener('click', () => {
    hamburger.classList.toggle('active');
    navLinksEl.classList.toggle('active');
  });
  navLinksEl.querySelectorAll('a').forEach(a => a.addEventListener('click', () => {
    hamburger.classList.remove('active');
    navLinksEl.classList.remove('active');
  }));

  /* ===== Theme toggle ===== */
  const themeToggle = document.getElementById('themeToggle');
  const themeIcon = themeToggle.querySelector('i');
  const savedTheme = localStorage.getItem('theme') || 'dark';
  setTheme(savedTheme);

  themeToggle.addEventListener('click', () => {
    const next = document.body.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';
    setTheme(next);
    localStorage.setItem('theme', next);
  });

  function setTheme(theme){
    document.body.setAttribute('data-theme', theme);
    themeIcon.className = theme === 'dark' ? 'fa-solid fa-sun' : 'fa-solid fa-moon';
  }

  /* ===== Scroll reveal ===== */
  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting){
        entry.target.classList.add('visible');
        revealObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.15 });
  document.querySelectorAll('.reveal').forEach(el => revealObserver.observe(el));

  /* ===== Animated stat counters ===== */
  const statObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting){
        animateCount(entry.target);
        statObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.5 });
  document.querySelectorAll('.stat-number').forEach(el => statObserver.observe(el));

  function animateCount(el){
    const target = parseInt(el.dataset.count, 10);
    const suffix = el.dataset.suffix || '+';
    const suffixFinal = target === 100 ? '%' : '+';
    const duration = 1200;
    const start = performance.now();
    function tick(now){
      const progress = Math.min((now - start) / duration, 1);
      el.textContent = Math.floor(progress * target) + (progress >= 1 ? suffixFinal : '');
      if (progress < 1) requestAnimationFrame(tick);
    }
    requestAnimationFrame(tick);
  }

  /* ===== Back to top ===== */
  document.getElementById('backToTop').addEventListener('click', () => {
    window.scrollTo({ top: 0, behavior: 'smooth' });
  });

  /* ===== Project sliders ===== */
  document.querySelectorAll('.project-slider').forEach(slider => {
    const slides = Array.from(slider.querySelectorAll('.slide'));
    const dotsContainer = slider.querySelector('.slider-dots');
    let index = 0;
    let timer;

    slides.forEach((_, i) => {
      const dot = document.createElement('span');
      if (i === 0) dot.classList.add('active');
      dot.addEventListener('click', () => goTo(i));
      dotsContainer.appendChild(dot);
    });
    const dots = Array.from(dotsContainer.children);

    function goTo(i){
      slides[index].classList.remove('active');
      dots[index].classList.remove('active');
      index = (i + slides.length) % slides.length;
      slides[index].classList.add('active');
      dots[index].classList.add('active');
    }

    function next(){ goTo(index + 1); }
    function prev(){ goTo(index - 1); }

    function startAuto(){ if (!prefersReducedMotion) timer = setInterval(next, 3500); }
    function stopAuto(){ clearInterval(timer); }

    slider.querySelector('.next').addEventListener('click', () => { next(); stopAuto(); startAuto(); });
    slider.querySelector('.prev').addEventListener('click', () => { prev(); stopAuto(); startAuto(); });
    slider.addEventListener('mouseenter', stopAuto);
    slider.addEventListener('mouseleave', startAuto);

    startAuto();
  });

  /* ===== Contact form (front-end demo) ===== */
  const contactForm = document.getElementById('contactForm');
  const formStatus = document.getElementById('formStatus');
  contactForm.addEventListener('submit', async (e) => {
    e.preventDefault();
    const lang = document.documentElement.lang === 'es' ? 'es' : 'en';
    const submitBtn = contactForm.querySelector('button[type=submit]');
    const submitLabel = submitBtn.querySelector('[data-i18n="contact.send"]');
    const originalLabel = submitLabel.textContent;

    submitBtn.disabled = true;
    submitLabel.textContent = translations[lang]['contact.sending'];
    formStatus.textContent = '';

    const payload = {
      name: contactForm.name.value,
      email: contactForm.email.value,
      message: contactForm.message.value,
    };

    try {
      const res = await fetch('/api/contact', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload),
      });
      if (!res.ok) throw new Error('Request failed');
      formStatus.textContent = translations[lang]['contact.formSuccess'];
      contactForm.reset();
    } catch (err) {
      formStatus.textContent = translations[lang]['contact.formError'];
    } finally {
      submitBtn.disabled = false;
      submitLabel.textContent = originalLabel;
    }
  });

  /* ===== Footer year ===== */
  document.getElementById('year').textContent = new Date().getFullYear();

});
